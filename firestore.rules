rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow public read/write to all collections for now to fix permission errors
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
